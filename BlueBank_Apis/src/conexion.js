const sql = require('mssql');

var config = {
    "user": "deisycuellarg",
    "password": "Deftones92*",
    "server": "deisycuellarg.database.windows.net",
    "database": "BlueBank ",
    "options": {
        "encrypt": true
    }
};

module.exports = config;